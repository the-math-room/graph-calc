{
  "name": "lambda-graph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "dev": "vite",
    "preview": "vite preview",
    "test": "tsc --project tsconfig.test.json && node --test .test-build/language.test.js"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.5.0",
    "vite": "^8.1.3"
  }
}
